<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.panthiya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Panthiya">
        <activity android:name=".makeAssingment"></activity>
        <activity android:name=".checkAssingment" />
        <activity android:name=".addMark" />
        <activity android:name=".TeacherRecordBook" />
        <activity android:name=".studentRegisterPage" />
        <activity android:name=".register" />
        <activity android:name=".aboutus" />
        <activity android:name=".profile" />
        <activity android:name=".activityDrower" />
        <activity android:name=".home_main_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>