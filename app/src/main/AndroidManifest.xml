<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.panthiya">

    <uses-permission android:name="android.permission.INTERNET">

    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Panthiya">
        <activity android:name=".edit_assingment"></activity>
        <activity android:name=".view_assignment_student_view" />
        <activity android:name=".student_home" />
        <activity android:name=".check_assignment" />
        <activity android:name=".view_upload_assignment" />
        <activity android:name=".login_page" />
        <activity android:name=".makeAssingment">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".addMark" />
        <activity android:name=".TeacherRecordBook" />
        <activity android:name=".studentRegisterPage" />
        <activity android:name=".register" />
        <activity android:name=".aboutus" />
        <activity android:name=".profile" />
        <activity android:name=".main_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>